<UserControl
    x:Class="Finanse.Elements.OperationCategoryTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Finanse"
    xmlns:data="using:Finanse.Elements"
    xmlns:models="using:Finanse.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="68">

    <Grid
        MaxWidth="600"
        Margin="16,0">

        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="Auto" />
            <ColumnDefinition
                Width="*" />
        </Grid.ColumnDefinitions>

        <data:CategoryIcon 
            Glyph="{x:Bind OperationCategory.Icon.Glyph}"
            Color="{x:Bind OperationCategory.Color}"/>

        <TextBlock
            Name="Category_OperationCategoryTemplate"
            Grid.Column="1"
            VerticalAlignment="Center"
            Text="{x:Bind OperationCategory.Name}"
            Margin="20,0,0,0"
            FontSize="16"
            Foreground="{ThemeResource Text}" />
    </Grid>

</UserControl>
