<UserControl
    x:Class="Finanse.WydatekTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Finanse"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="70">

    <Grid MaxWidth="600">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid Name="Ikona" Height="50" Width="50">
            <Ellipse Fill="#e64231"/>
            <TextBlock FontFamily="Segoe MDL2 Assets"
                       Text="&#xE14D;"
                       FontSize="24"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Foreground="White"/>
        </Grid>
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="{x:Bind Wydatek.Title}"
                       Margin="20,0,0,0"
                       FontSize="16"
                       Foreground="White"/>
            <TextBlock Text="{x:Bind Wydatek.Type}"
                       Margin="20,0,0,0"
                       FontSize="12"
                       Foreground="White"/>
        </StackPanel>
        <TextBlock Grid.Column="3" Text="{x:Bind Wydatek.Cost}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Right"
                   FontWeight="Bold"
                   Foreground="{StaticResource RedColorStyle}"/>
        <!--<CalendarDatePicker Grid.Column="4" Date="{x:Bind Wydatek.Date}" />-->
    </Grid>
</UserControl>
